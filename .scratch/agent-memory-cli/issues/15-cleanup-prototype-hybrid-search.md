# 15 — Clean Up Prototype Hybrid Search Script & File

**What to build:** Removes throwaway file `src/prototype-hybrid-search.ts` and deletes the `prototype:hybrid` script entry from `package.json` now that hybrid search scoring is fully integrated and tested in core `NeuronMemory`.

**Blocked by:** None — can start immediately.

**Status:** resolved

- [x] `src/prototype-hybrid-search.ts` is deleted.
- [x] `prototype:hybrid` script removed from `package.json`.
- [x] `npm run build` and test suite run cleanly.
